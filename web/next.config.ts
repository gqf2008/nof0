// This project now uses Vite for the frontend build. The legacy Next.js
// configuration file is kept empty so tooling that still references it does
// not fail, but it no longer contributes any behavior.
export {};
