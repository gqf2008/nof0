// Legacy Next.js entry point retained for reference. The Vite-based build does
// not load this file, but we keep a minimal export so TypeScript continues to
// compile the folder without pulling in Next.js types.
export default function LegacyRootLayout() {
  return null;
}
